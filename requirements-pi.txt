# Raspberry Pi specific dependencies
# Install on Pi only: pip install -r requirements-pi.txt
# Requires Pi Camera Module 3 (uses libcamera/picamera2 stack)

picamera2>=0.3.17
libcamera>=0.0.5
RPi.GPIO>=0.7.1

# ML inference on Raspberry Pi
# --------------------------
# ONNX Runtime is the recommended backend (works reliably on Pi 4 aarch64):
#   pip install onnxruntime
#
# You also need the exported ONNX model file. On your dev machine, run:
#   pip install torch torchvision
#   python scripts/export_to_onnx.py
# Then copy models/mobilenet_v3_small.onnx to the Pi.
#
# PyTorch is NOT recommended on Pi 4 (causes "Illegal instruction" errors
# on Cortex-A72 aarch64 Bookworm due to unsupported CPU instructions).
#
# To skip ML analysis entirely:
#   python main.py --no-analysis
