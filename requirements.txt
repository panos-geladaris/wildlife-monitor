# Core dependencies (all platforms)
APScheduler>=3.10.4
Pillow>=10.0.0
Flask>=3.0.0
Flask-CORS>=4.0.0
python-dotenv>=1.0.0
PyYAML>=6.0

# Testing
pytest>=7.0.0

# ML dependencies (required for analysis module)
# Note: On Raspberry Pi, install torch separately with:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
# torch>=2.0.0
# torchvision>=0.15.0
opencv-python-headless>=4.8.0  # Headless version for Pi

# Numpy - use system version on Pi to avoid picamera2 conflicts
# pip install numpy==$(python3 -c "import numpy; print(numpy.__version__)")

# Audio classification (optional - system degrades gracefully without them)
panns-inference    # General animal sound detection (PyTorch-based)
birdnet            # Bird species identification (TFLite-based)
