# Core dependencies (all platforms)
APScheduler>=3.10.4
Pillow>=10.0.0
Flask>=3.0.0
Flask-CORS>=4.0.0
python-dotenv>=1.0.0
PyYAML>=6.0

# Testing
pytest>=7.0.0

# ML inference (ONNX Runtime - recommended, works on all platforms including Pi)
onnxruntime>=1.16.0
numpy>=1.24.0
opencv-python-headless>=4.8.0

# PyTorch (optional - only needed for model export or as fallback backend)
# pip install torch torchvision
