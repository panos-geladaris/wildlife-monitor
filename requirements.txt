# Core dependencies (all platforms)
APScheduler>=3.10.4
Pillow>=10.0.0
Flask>=3.0.0
Flask-CORS>=4.0.0
python-dotenv>=1.0.0
PyYAML>=6.0

# Testing
pytest>=7.0.0

# ML (install separately: pip install torch torchvision)
# torch>=2.0.0
# torchvision>=0.15.0
